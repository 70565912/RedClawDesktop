# Development Log

## 2026-09-22 — Prepare v0.1.4 Developer Preview

- Version metadata is 0.1.4 in CMake and vcpkg. Bilingual README, the task ledger, project state and [release notes](../releases/v0.1.4.md) point at this preview. The README image is the current connection homepage: two code columns, masked password fields, and collapsed More settings. v0.1.3 and older passwordless releases are rejected. The publisher builds Release and checks the ZIP before creating the tag. Asset hashes are recorded after that upload. No signed installer is included.

## 2026-09-22 — Source synchronization and connection UI delivery (X00-T24)

- Synchronized main through upstream `18b80b4` (Debug-default workspace control) and `6b6e930` (independent playback window and physical-pixel input). The local input DPI change was identical to upstream. Preserved both development-log histories when restoring the local UI changes; the local `.dpi-bak` file remains outside the commit. No branch rewrite or force push.
- Updated both READMEs to single-entry password setup, the connection guide, project state and task ledger. The compact connection UI remains within its existing authentication and DPAPI formats.
- Earlier operator-authorized local startup rebuilt/published Debug and reused the protected Host credential. Startup reached `dht_waiting / offer_ready`, DHT reachable, with no reported error. This source-sync pass preserves that running instance; its startup evidence does not qualify the subsequently merged binary.
- Merged-source validation: `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop -NoPublish` passed, followed by guarded builds of the UI, runtime-options and remote-input test targets. Native Windows `ConnectionEntryPage.*:ConnectionFlowModel.*:PlaybackWindowLifecycle.*` passed 20 cases; `RuntimeOptions.*` passed 15; remote-input session/geometry tests passed 14 (49 total, zero skips). Evidence: `build/reports/github-sync-20260922/`. Existing main.cpp getenv deprecation warnings remain. The previously documented broader LocalControlOutput/Protobuf failure was not requalified; physical DPI/input and remote-session acceptance were not repeated.
- Delivery scope is source and documentation on GitHub main. The published v0.1.3 downloadable assets remain unchanged; no new release package or running-Host replacement is part of this source publication.

## 2026-09-22 — Host input uses the capture desktop's physical pixels

- Host `SendInput` absolute positions follow the calling thread's DPI context. The process is otherwise DPI-unaware, so `GetSystemMetrics` virtual-screen size is smaller than DXGI/WGC physical capture pixels and the cursor does not track the displayed frame one-to-one. Each injection batch now sets per-monitor DPI awareness v2 on that thread for both `SendInput` calls, then restores the previous context. Controller coordinates stay normalized `0..65535`. No wire schema or capability change; updating the Host is sufficient.
- This is a static contract correction. The peer Host was not replaced with this binary, and physical cursor tracking on that machine was not requalified.

## 2026-09-22 — Playback window is independent of the main window

- The playback window titled `RedClaw` is created without a `QWidget` parent. Parenting it to `RedClaw Desktop` with `Qt::Window` made it a Windows owned window, so it stayed above the main window and disappeared whenever that window was minimized. The two top-level windows can now be activated separately. Closing the playback window still returns to the main window and does not quit the process. The main window still deletes the playback window when the GUI shuts down.

## 2026-09-22 — Host replacement must keep a session-independent rollback

- Replacing a live Host while its session is the only recovery path stopped the peer and did not bring it back. The Controller then stayed at `connected=false` with `remote_revision=0` and `fetch_hits=0`: the Host was not publishing, rather than publishing but failing ICE. Restarting only the Controller does not publish a Host session.
- The remote terminal, workspace control and file transfer all ride that Host session. A rebuild scheduled after the stop also lacked the interactive desktop and the original compiler environment, and re-splitting the saved command line can make the new process exit immediately.
- Rule, now in [AGENTS.md](../../AGENTS.md) and the [remote result contract](../../.agents/skills/remote-agent-result-contract/SKILL.md): finish the new build first, arm a task that does not depend on the current session and can relaunch the previous program if the new one does not publish, and only then stop the old Host. Treat the replacement as done only after a new Host session record is visible. If that rollback cannot be armed, leave the old Host running and report the replacement blocked.

## 2026-09-22 — Compact connection homepage (X00-T24)

- Left/right columns now each contain code, password and role action. Password and peer-code fields share one stylesheet; Save/Forget remain compact, independent Show/Hide controls live inside each password field. Removed confirmation input and separate password status. Permissions, Agent configuration and network options share the default-collapsed More settings panel without changing authorization values.
- `ConnectionPasswordPanel` remains the credential owner and exposes two row widgets to `ConnectionEntryPage`. Single-entry saves stay asynchronous; successful saves clear/mask local input, failed saves retain it, and pending edits prevent waiting with an old verifier. Host storage still contains only verifier material. Revealing remembered Client passwords, masking on peer change/forget/start, cursor/selection preservation and running-session locks are covered. No protocol, authentication or persisted format changes.
- Changed `src/ui/connection_entry_page.cpp`, `connection_password_panel.{h,cpp}`, `gui_shell.cpp`, focused UI tests and the connection instructions. Full column Tab order is explicitly linked, including embedded visibility buttons. Native Qt initially used bullet masking and compound-widget traversal did not honor the complete column sequence; the final implementation fixes both and regression assertions cover them.
- Validation: guarded test-target build and `redclaw_ui_connection_flow_tests --gtest_filter=ConnectionEntryPage.*:ConnectionFlowModel.*`, using `QT_QPA_PLATFORM=windows`, passed 19/19 at each `QT_SCALE_FACTOR=1` and `1.5`. Tests cover empty rejection, single input save, DPAPI storage/failure, remembered/forgotten peer passwords, dirty gate, masking/reveal/cursor/selection, busy locks and full Tab chain. Isolated native page fixtures at logical 800×600 and 900×680 fit without horizontal overflow; reviewed screenshots use test-only passwords. Evidence: `build/reports/compact-connection-ui/final-{1,1.5}.{log,xml}` and `connection-*.png`. These are page-fixture captures, not screenshots of the running Host or a new remote session.
- Build/publication: `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop -NoPublish`, then `publish.ps1 -Configuration Debug -PublishDirectory release/Debug-CompactConnection-20260922`. Build and publication succeeded; published/build EXE SHA256 matched (`A445D86DC821291F9C1D4AA2594714323EAB3ED0DA48CD7522FBEC606EC5F280`), candidate `--help` exited 0, and `git diff --check` passed. Separate candidate preserves the occupied `release/Debug` directory and existing GUI/runtime PIDs 6112/23148. No live Host replacement, remote deployment or Git commit/push. Authentication/network end-to-end tests were not repeated for this UI-only change; physical-monitor DPI changes and remote-session qualification are outside this validation.

## 2026-09-21 — Mandatory connection password (X00-T23)

- Completed local implementation: independent SCRAM-SHA-256-based authentication gates every desktop/workspace channel. Host requires a confirmed password; Client supplies the Host password, connects automatically only on success, remembers successful credentials by machine code and supports changing/forgetting them. Waiting/connected sessions lock password editing. Passwords preserve case and whitespace; there is no complexity rule or passwordless fallback. UI password derivation runs in a worker so saving does not block its event loop.
- Interface changes: Control protobuf appends auth capability/message fields 85–87 and enum 26 without renumbering existing fields. Proofs bind random nonces, roles, both DTLS fingerprints and per-connection epochs. The local-only `RCD-LOCAL-AUTH-V1` parent/child pipe message and optional current-user DPAPI file bootstrap are separate from network frames. DPAPI protects Host verifiers and remembered Client credentials; plaintext is excluded from arguments, settings and restart snapshots. See [connection password contract](../architecture/connection-password-v1.md).
- Recovery: wrong passwords terminate the attempt, not the online Host. Five consecutive failures preserve a 30-second cooldown across transport resets; each attempt has a 30-second authentication deadline. Qualification exposed and fixed two recovery issues: rejected pre-auth sessions must immediately publish a fresh standby Offer, and a never-authenticated Client must be allowed to replace a cached Offer with the same Host's strictly newer generation. Already-authenticated recovery, stale/failed generation rejection and other feature authorization remain enforced. No duplicate Control decode was added to the authenticated hot path.
- Files: new `src/security/.../connection_auth.{h,cpp}`, `src/ui/connection_password_panel.{h,cpp}`; integration in `net_module`, `connection_negotiation`, protocol schema/codec, Runtime startup/options and connection GUI; credential generation and validation/startup scripts; focused security/net/UI/service regressions, README and runtime documentation. Earlier Agent approval removals and unrelated launcher/docs changes remain preserved and uncommitted.
- Focused checks passed **104 cases**, zero skips: auth 9, native gated ICE/DTLS transport 3, native Windows connection GUI 18, transport lifetime/zero-delay 7, Control protocol 21, negotiation/publication 46. Coverage includes correct/wrong/empty/Unicode/changed passwords, exact whitespace, DPAPI corruption, settings write failure, replay across fresh epochs, tamper, DTLS mismatch, timeout, reconnect, cooldown, premature business frames and both legacy-capability directions. An existing zero-delay fixture raced candidate gathering; it now waits for both SDP and candidate availability. Its focused recheck passed; the prior 22/45/90-second and never-delivered cases also passed. Offscreen-font layout assertions failed, while the supported native Windows platform passed unchanged size requirements.
- Final `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop` built and published successfully. Build/published EXE SHA256 matches `d6ca158a834713367a268ef409e0958f61406fc5135a38f1e983e1b7ef3670f2`; published help exits 0. Missing, corrupt and wrong-role startup credentials each fail closed with exit 78. One intermediate publication met a test-owned DLL lock; after stopping only those owned processes, publication succeeded. Final build log: `build/auth-qualified-build.log`.
- Real local desktop gate passed on the final binary: `build/reports/connection-password/local-dual-gui-20260921_220106_986/result.json` and `controller-live-view.png`. Host sampled native 1680×1050, captured=671, synthetic=0, encoded/transmitted=201 with zero encode failures. Controller runtime sampled received/reassembled/pipe-written=62; the later GUI sample decoded=250, presented=246, decode/present failures=0. These are independent timestamped samples, not equal-time conservation counters or a performance comparison.
- Actual binary matrix passed: `build/reports/connection-password/matrix-20260921-220106/result.json`. Wrong password and new-Host/old-Client plus old-Host/new-Client reject without exposed business frames; the new endpoint reports explicit incompatibility. Online DHT recovery kept the same Host process, rejected the bad Client, then authenticated a fresh correct Client and transmitted real frames. Old candidate SHA256 is recorded in the receipt. Earlier failed matrix runs are retained as evidence for the repaired recovery paths.
- Credential audit passed across logs, JSON diagnostics, coordination/settings files, four live test process command lines and one matching maintenance restart snapshot; no plaintext password or Host verifier keys found. Receipts: `privacy-running.json` / `privacy-audit.json` under the final GUI report and the matrix privacy audit. New credential/helper matrix scripts pass focused analyzer checks; broader modified-script lint retains pre-existing warnings and false positives for ciphertext path parameter names, with no new plaintext-conversion error. `git diff --check` passed.
- Scope: final Debug candidate only. Existing user GUI/runtime PIDs 8888/21480 were preserved; test-owned processes exited. No Git commit/push, remote deployment, installed-instance replacement or physical cross-machine qualification. Existing v0.1.3 release assets were not changed; a peer without this authentication capability must upgrade before using this candidate.

## 2026-09-21 — Agent execution without task or command approval (X00-T22)

- Contract: after Host authorizes Agent access to a registered project, current production providers execute submitted tasks without an additional task/command approval. Cursor starts its existing noninteractive child process directly; Codex uses `approvalPolicy=never` for new threads, resumed threads and each turn, retaining `workspace-write`. Unexpected provider approval requests are declined with a diagnostic rather than leaving the task waiting for user input.
- Existing Agent v1 approval messages, broker handling and Controller cards remain available for supported older peers; no wire schema or capability version changes. Current providers advertise both approval flags as false. Project authorization, account readiness, task isolation and interrupt remain enforced.
- Files: `src/agent/src/{cursor_agent_provider,codex_app_server_provider}.cpp`, `src/ui/agent_conversation_panel.cpp`, the corresponding provider and conversation-panel tests, Agent bridge architecture/integration runbook, this log and `MODULE_KANBAN.md`.
- Validation: test-only guarded build passed; `redclaw_agent_provider_tests --gtest_filter=-AgentProviders.RealProviderReadinessWhenExplicitlyEnabled` passed 20 cases, `redclaw_agent_remote_agent_broker_tests` passed 20, and `redclaw_ui_connection_flow_tests --gtest_filter=AgentConversationPanel.*` passed 23 with `QT_QPA_PLATFORM=offscreen` (63 total, no skips). Coverage includes launch failure/retry, registered chat resume, interrupt/follow-up, unexpected approval decline/reply failure and legacy approval UI/broker lifecycle. The installed Codex CLI's generated schemas confirm `never` on start/resume/turn and `workspace-write` on start/resume.
- `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop` built and published successfully; published `--help` exited 0, build/published EXE SHA256 matched, and `git diff --check` passed. Evidence is under `build/reports/agent-no-approval/`. No real-account model task, physical mixed-version session, live runtime replacement or peer deployment was performed; the execution endpoint must run the new build to apply this behavior.

## 2026-09-21 — Initial capture bounds and remote keyboard ordering (M01-T04 / M05-T04)

- Files: `src/main.cpp`, `src/capture/include/redclaw/capture/capture_module.h`, `src/ui/remote_input_capture.{cpp,h}`, new `src/ui/clipboard_shortcut_state.h`, `tests/ui/connection_flow_tests.cpp`, `tests/capture/capture_recovery_cursor_tests.cpp`, `docs/architecture/remote-workspace-v013.md`, this log and `docs/runtime/MODULE_KANBAN.md`.
- Host now retains capture selection as normalized bounds until an actual frame supplies dimensions; encoding and input mapping resolve the same selection. Previously, a catalog/initial frame smaller than a later captured frame could leave a full-desktop selection cropped to the old pixel extent. Explicit crops and rollback preserve their normalized selection. This is an internal capture contract only, with no wire schema or capability change; no peer version equality is required.
- Controller drains queued input before publishing its pressed-key snapshot. Previously, a released-modifier snapshot could reach Host before queued slash/C/V transitions and release Shift/Ctrl ahead of the chord. Remote Ctrl+C/Ctrl+X also records the current local clipboard sequence: unchanged local data leaves subsequent Ctrl+V as ordinary remote input; a new local copy restores on-demand local transfer. Existing focus, injected-key filtering, pause and transfer gates remain active.
- Validation: the focused UI executable ran 17 input/order/clipboard/navigation cases and the capture executable ran two selection/recovery/rollback cases; all 19 passed with zero skips. The ordering fixture feeds the actual Controller queue into the Host input session and an in-memory injector, including multi-batch and send-failure boundaries; it does not claim physical hook or target-application consumption. Test startup initially needed the matching VS2022 environment, and a Windows `small` macro collision in one new test was corrected before its successful build.
- `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop` completed and published the candidate. Published `--help` exited 0, the app SHA256 matches the build output, and `git diff --check` passed. Generated receipts are under `build/reports/initial-region-keyboard-fix/`. No live runtime replacement, peer deployment, GitHub operation or physical dual-machine requalification occurred. The user's original field symptom is not claimed closed by these local regressions alone.

## 2026-09-21 — Debug workspace control default (X00-T21)

- GUI and runtime options now share a build-specific workspace-control default: enabled in Debug, explicitly enabled in Release. Current-user pipe authentication, peer capability checks and operation authorization remain unchanged; no public listener or remote privilege is added.
- Added build-specific default assertions and preserved explicit-enable / missing-name coverage. Debug main build passed through `build.ps1 -SkipConfigure -NoPublish -Target redclaw_desktop`; `RuntimeOptions.*` passed all 15 cases in each of Debug and Release. The broader Debug test executable failed afterward in `LocalControlOutput` with a Protobuf has-bit consistency / access-violation error; that unmodified area was not repaired or counted as passing. Whitespace checks passed. No active-runtime replacement, GUI listener probe or remote transfer was performed for this source checkpoint.

## 2026-09-21 — Workspace control API (X00-T21)

- Added opt-in current-user `RedClawDesktop.WorkspaceControl.v1` IPC and `invoke-workspace-control.ps1`, routed through shared terminal/transfer coordinators and the existing process-verified runtime pipes/P2P connection. Requests have explicit version/instance identity, conflict-aware deduplication, bounded operation/event/output history, cursor gaps and unknown-result states. Hidden windows work; no public listener, separate Shell/transfer engine or per-command confirmation was added.
- Terminal capability 2 uses fixed PowerShell integration and a child-only anonymous receipt pipe for execution/output/completion. Execution retains the existing Shell scope, exclusively owns input and returns PowerShell success separately from historical `last_native_exit_code`. Cancellation restores the Shell's inherited Windows Ctrl+C handling and waits for prompt/exit evidence. ConPTY small writes are combined into the existing 16 KiB chunk without increasing its 1 MiB queue. Clipboard capability 3 adds bidirectional verified snapshot read/write and independent focus-guarded paste; file capability stays 1, with shared GUI/API selections, progress, item journals and cancellation. New fields/messages are common-capability gated.
- Serial Debug main build succeeded. The selected workspace/UI/Agent/smoke run passed all 22 suites (301 cases, zero skips); the separate runtime-options suite (22 cases) and script parsing/lint passed. Earlier qualification exposed premature-open completion, inherited Ctrl+C suppression and excessive per-write ACK cost; their corrected native/GUI runs passed. New native tests cover hidden/shared Shell state, success/failure/native status, cancellation, abrupt Shell exit, reconnect without replay, bounded output, six clipboard formats/files/folders, focus invalidation and legacy paste through injected adapters.
- Final candidate real DHT/ICE capture/encode/decode/presentation passed with zero synthetic frames and decode/presentation failures. All 23 script checks passed, including deduplication, shared variables, wait without cancellation, cancellation, simultaneous output/transfer, expired history and a 64 MiB file/directory upload/download with matching SHA256 and preserved empty directories. The initial uncoalesced candidate timed out waiting for the same output workload; the final candidate completed it in 29.45 seconds. This is a functional/backpressure observation, not a controlled overall-performance comparison.
- Concurrent-load baseline/load/recovery observations: display 31.14/15.40/7.03 FPS; Controller maximum heartbeat 43.00/65.22/36.10 ms; four-process private-memory peaks 448.3/519.5/456.2 MiB. Terminal history remained at most 1 MiB; sampled Host media active/pending depth was zero. The scene was not fixed, so retain the frame-rate drop without attributing it to a specific stage or claiming X00-T20 is repaired.
- Both final-candidate mixed-version directions passed. New Controller/old Host negotiated terminal/file/clipboard 1/1/2 and passed eight legacy stream/browse/new-operation-rejection checks. Old Controller remained alive while the owned old Host rolled into the candidate over online DHT/ICE: instance adoption took 43.05 seconds, subsequent convergence 26.30 seconds, and real media resumed. Tests created and closed their own isolated processes. Delivery is an independent Debug candidate; the user's running instance, system clipboard and GitHub Release binaries remain unchanged. Actual application paste is an optional evaluation, not an automatic release requirement.
- The independent 319-file candidate archive passed SHA256, extraction, all 318 manifest-file checks and packaged command-entry startup (exit 0). Its notes identify the Debug development-runtime dependency; no public release or active-runtime switch was performed.
- Source synchronization checkpoint: the operator authorized documentation updates, commit and GitHub push. The source scope includes the API implementation, scripts, focused tests, contract, examples and qualification record. Feature qualification covers the successful Debug build and 23 automatic suites / 323 cases before integration of the concurrent Host media updates. Source synchronization includes those five existing remote commits through `2d9fbca`, with both endpoints' diagnostic histories retained. The merged-source build/regressions are a separate checkpoint; the earlier candidate archive is not relabeled as a build of the merged source. No new binary release or runtime switch is implied.
- Merged-source verification passed: one serial Debug main build through `build.ps1 -Configuration Debug -SkipConfigure -NoPublish -Target redclaw_desktop`, followed by nine focused CTests covering adaptive sending, real software-codec delivery, recovery, transport/timing, frame tracing, terminal sessions, file runtime and the shared workspace API. All nine passed with zero skips in 62.22 seconds. Document links and staged whitespace checks passed. Prior GUI/mixed-version/archive evidence remains tied to the original candidate; this source checkpoint did not rerun that complete matrix or replace the active runtime.

This public log records release-level changes. Machine-specific paths, addresses, runtime signaling, credentials, and raw evidence are intentionally excluded.

## 2026-09-21 — Adaptive sender long-session closure (X00-T20, local deployment)

- The first latency-bounding follow-up removed receiver-rate self-lock, reset-from-now cadence drift, probe-boundary pauses and keyframe-driven FIFO growth. Its 45-second trace improved to 27.411 FPS with two gaps above 300 ms, but the required five-minute soak found a second failure: transient media feedback repeatedly reduced pacing from about 8.5 Mbps to 162 kbps, ten-second transmit fell to 1.3 FPS, oldest pending age reached about 1.01 seconds and the queue target incorrectly grew to about 806 ms / 21 frames despite zero sampled loss.
- Final corrections make pressure confidence and retreat path-derived instead of line-tuned: a probe under pressure first returns to its confirmed baseline; media and RTT pressure require repeated observations; destructive backoff uses the median of recent valid delivery samples rather than one low/aggregated batch; and token-limited frame duration is not learned as local FIFO service. Existing loss, resource, dependency and bounded-memory protections remain. No GOP, VBV, resolution, encoder quality, receiver depth or wire schema changed.
- The affected adaptive-sender target passed once for each changed revision, including new regressions for uncertain queue feedback, probe cancellation, token-limited service and non-cascading backoff. Each Debug candidate built once through `build.ps1 -SkipConfigure -Target redclaw_desktop -NoPublish`; no full matrix was repeated. Independent full-directory replacements installed 337-file candidates, retained complete rollback directories and verified both manifests. Local commits are `cf9e368` and `4c1bbb2`; no GitHub push was performed in this closure phase.
- Final evidence: 68-second status window, 28.721 FPS overall and 27.3–28.4 FPS across seven ten-second windows, fixed 11.27 Mbps pacing, one-frame / 33.333 ms FIFO and zero loss/backoff/failures. The accompanying 45-second trace has 1256 contiguous successful frames, 27.918 FPS, zero gaps above 300 ms, 212.85 ms maximum gap and 161.99 ms mean keyframe pacer time. The 300.511-second soak remained connected on one runtime at 27.673 FPS; all 31 ten-second windows were 26.2–28.4 FPS, pacing adapted within 6.792–11.826 Mbps with one non-cascading backoff, and no loss, deadline, dependency drop, recovery, rejection, encode or send failure advanced. The FIFO target remained one frame / 33.333 ms. Evidence `EV-X00-T20-FINAL-SHORT-20260921`, `EV-X00-T20-FINAL-TRACE-20260921` and `EV-X00-T20-FINAL-SOAK-20260921`.

## 2026-09-21 — Adaptive sender latency regression correction (X00-T20, candidate)

- Post-deployment same-instance observation confirmed the first adaptive candidate regressed responsiveness: capture remained about 59 FPS while encode/send fell to 10.3–16.8 FPS; the Host FIFO reached 14 pending frames, the oldest pending frame reached 426 ms and the learned target expanded to about 942 ms. Sampled loss remained zero and no stream failure counter advanced. This proves sender-side latency accumulation, not a complete end-to-end latency attribution.
- Corrected three coupled mechanisms: receiver decode throughput no longer echoes back as a second full-frame rate limiter; full-frame pacing now uses a cumulative fractional clock with bounded catch-up; and keyframe service history no longer expands the ordinary P-frame FIFO target. A completed probe interval now returns ordinary traffic to its previously confirmed rate while probe feedback is pending instead of stopping the media stream.
- One directly affected automatic target passed, including new probe-continuity and latency-bounding cases plus the existing generated path/timing regressions. The Debug main candidate then built once through `build.ps1 -SkipConfigure -Target redclaw_desktop -NoPublish`. The first sandboxed build attempts made no compiler progress; the actual compiler run initially exposed a mixed VS2022 compiler/newer-STL environment and passed after importing a matching VS2022 environment only for the child process. No system environment was changed. Deployment and field comparison remain separate from this candidate evidence.

## 2026-09-21 — Adaptive sender candidate (X00-T20, local validation complete)

Deployment follow-up: the operator subsequently authorized commit/push and local Host replacement. Implementation `9324053` was pushed to GitHub; the already verified Debug binary was packaged without another build/test sweep. The independent worker installed the complete 337-file bundle, retained the complete rollback directory and restarted Host. Installed and rollback manifests both verify; operational arguments are preserved except the coordination build identifier. The first post-start check was waiting for the peer; a later read-only check confirmed phase=streaming, connected=true, capture=5698, encode=1901, transmit=1888, synthetic=0 and no reported runtime error. Deployment/startup/reconnection are confirmed, not matched-scene FPS improvement or incident closure. Evidence `EV-X00-T20-ADAPTIVE-DEPLOY-20260921`.

- Implemented one feedback-driven delivery/probe/window authority, accumulated pacing/cadence timing and a bounded reusable encoded-frame FIFO. New local typed contracts expose application-limited/probe attribution, decision revisions, adaptive queue targets/reasons and pre-encode memory reservations. Encoder output checks reserved storage before allocation and requests IDR after a resource rejection. Existing wire schemas, encoder quality/GOP/VBV, two-slot receiver capacity and runtime deployment remain unchanged.
- Baseline: `EV-X00-T20-FRAME-TRACE-20260920` (19.384 FPS; keyframe pacer mean 440.41 ms including 99.45 ms wait overshoot). The candidate removes the encoder/20 Mbps sending ceiling, learns feedback cadence and timer lateness, freezes queue tolerance during congestion, and prevents repeated ACKs from replaying stale pressure. Probe confirmation requires an intact tagged interval; loss, expiry or sent-metadata eviction cannot become a later successful probe merely because the short loss window expired.
- Selected six affected CTest targets once: four passed initially and two exposed a queue-tolerance defect and old ACK/demand fixture assumptions. Only failed/newly affected cases were rechecked after fixes. Final evidence covers seven new algorithm cases (12 seeded continuously varying paths, contention, random loss, ACK aggregation, collapse/recovery, idle demand, quantized waits and cadence) plus two true software-H.264/fragment/reassembly/two-slot/decoder cases. The codec FIFO delivered 24/24 frames, no errors/dependency drops, reused payload blocks and grew to three pending frames within its resource bound; resource rejection recovered through an independently decodable IDR. Existing unrelated test results were reused, not a full matrix rerun.
- Main Debug candidate built through `build.ps1 -Configuration Debug -SkipConfigure -Target redclaw_desktop -NoPublish`; the final probe-integrity fix required only an additional incremental relink through the same entrypoint. Both succeeded. Initial test compilation was blocked by sandbox cache/subprocess access and mixed VS2022/compiler-vs-newer-STL environment; matching VS2022 in the child build process resolved it without persistent environment changes. No push, publication, deployment, startup, restart or peer task. Evidence `EV-X00-T20-ADAPTIVE-20260921`; details and remaining matched-scene field validation are in the [diagnostic handoff](../testing/desktop-latency-quality-investigation-20260915.md). Synthetic timing properties do not establish a real Host FPS/latency improvement or close the incident.

## 2026-09-20 — Authorized bounded Host frame trace (X00-T20, deployed diagnosis)

- Local-only typed trace metadata travels with the captured/queued frame into a runtime-owned recorder. A versioned local sidecar request opts into 1–120 seconds / 4096 frames; overflow is explicit, capture stops automatically and numeric CSV export waits for admitted frames to drain. No image/input payload, online schema, admission, rate, recovery or queue changes; no capture/encode/send-path disk writes.
- Four new focused cases passed once: disabled/bounded lifetime, sidecar/export, same-frame token/send attribution and other blocking predicates. One Debug main build through `build.ps1 -NoPublish` passed; no full matrix rerun. The complete 337-file diagnostic bundle was installed by the independent upgrade worker, with both installed and rollback manifests verified. Host reconnected; launch settings are preserved except the coordination build identifier. This is an uncommitted local diagnostic build based on `f830e12`, with source hashes retained locally; no GitHub operation.
- The 45-second trace contains 873 contiguous, successfully sent frames / 1368 complete fragments, no overflow: 19.384 FPS, 2463.85 kbps encoded payload / 2483.31 kbps application wire output against 3806 kbps pacing. All 15 send-completion gaps above 300 ms end at keyframes (339–574 ms). Natural 60-frame keyframe intervals span 2.788–3.329 seconds; keyframe capture/encode averages are 15.62/18.91 ms, but pacer duration averages 440.41 ms, including 418.93 ms token wait (95.1%). The following queued frame waits 398.31 ms on average, directly linking sender backpressure to later capture-timestamp jumps and reduced encode admission.
- Timed waits overshoot their requested durations by 99.45 ms per keyframe on average, 6.58 seconds total across the window; this includes wakeup and mutex reacquisition, not proof of an OS-timer-only cause. Buffered/channel/probe waits are zero; in-flight waits total 417 ms and occasionally add to a stall. A separate loss/backoff event occurs after this trace, so this does not declare the network universally fault-free. Encoder VBV defaults versus the small pacer burst remain a source-derived configuration risk, not driver readback. Next: precise sender timing plus a quality-preserving encoder/frame-budget contract; no functional tuning implemented and incident remains open. Evidence `EV-X00-T20-FRAME-TRACE-20260920`.

## 2026-09-20 — Remaining periodic stall, read-only follow-up (X00-T20)

- A fresh 50-second interval transmitted 1131 frames (22.62 FPS), with ACK +1529, 19 keyframes and no new pacer drops, budget rejections or adaptation backoff. A 490-second same-instance history also retained fresh feedback across a natural capture generation change, without the previous sequence-reset freeze.
- Five sampled encoder waiting states lasted 226–353 ms while both pacer slots were occupied, capture remained active and feedback stayed fresh. This locates Host-side admission backpressure, but ten-second summaries do not identify each stalled frame or partition token, native-send and scheduler waits. Periodic keyframe size versus NVENC's implicit two-second VBV and the small sender burst budget is the leading hypothesis, not a verified sole cause.
- Saved only diagnostic evidence and updated sanitized documentation; no product edits, build, test rerun, deployment, restart, tuning or GitHub operation. Next instrumentation needs bounded per-frame correlation before selecting a fix. Evidence `EV-X00-T20-STUTTER-F-20260920`; incident remains open.

## 2026-09-20 — Capture feedback and NVENC recovery correction (X00-T20, deployment history)

- Authorized follow-up preserves transport sequence across source/capture resets; `DesktopMediaSendPacer::reset()` now defaults to in-epoch recovery, and the two connection-epoch owners explicitly request sequence reset alongside the estimator reset. Receiver replay gates and online schemas are unchanged.
- NVENC initialization sets and reads back `forced-idr=1`, independently of latency tuning, and fails initialization if the contract cannot be applied. Recovery requests now sever predictive references instead of merely requesting an intra picture. No bitrate, dimensions, GOP, queue capacity, connection or authorization configuration changes.
- Three focused recovery cases passed, including repeated default resets through both feedback endpoints and a genuine epoch reset. One developer-selected local NVENC H.264 test passed: two early recovery requests each produced an IDR that a fresh decoder decoded without earlier frames. This hardware-specific case is excluded from portable CTest/release requirements. Reuse the prior 15 timing/adaptation cases and media-transport CTest; no full matrix rerun. One Debug main build through `build.ps1 -NoPublish` passed.
- Pushed implementation `f75ac19`, staged and verified the complete 337-file bundle, then completed the authorized independent Host upgrade. The installed and rollback manifests both verify. Existing launch settings are preserved; only the coordination build revision changes. The new Host reconnected. A 55-second read-only observation contains a 40-second same-instance counter interval: capture +2378 (59.45 FPS), encode +855 (21.38 FPS), transmit +853 (21.32 FPS), media ACK +1148, with no added capture/encode/transmit failures, synthetic frames, pacer deadline drops or frame-budget rejections. Current-instance keyframe refresh totals are 3 attempts / 3 successes, not three new requests inside this window.
- This is not a matched-scene performance trial or incident closure. Capture generation stayed unchanged during the window; physical capture-recovery continuity and long-session effectiveness remain unverified. One independent RTT queue spike (129 ms) still caused a temporary backoff, so do not suppress real congestion signals or claim all 3–4-second stalls are solved. Evidence `EV-X00-T20-RECOVERY-DEPLOY-20260920`; identities, raw logs and rollback paths remain local under the remote-result contract.

## 2026-09-20 — Post-deployment slowdown diagnosis (X00-T20)

- Read-only same-instance windows separate temporary recovery (~22.6 FPS / 3806 kbps pacing) from renewed degradation (~4.25 FPS / 638 kbps). Media feedback froze across capture generation changes without a connection epoch reset; RTT continued. Frozen ACK bitrate/queue and zero in-flight accounting are not current network-health measurements.
- Found local capture availability/region reset paths clearing the connection-level transport sequence while both feedback endpoints retain their monotonic sequence gates. The geometry path already preserves sequence. Also found NVENC's forced I-frame request is not forced IDR: the local FFmpeg backend defaults to FORCEINTRA. A window recorded 55 refresh attempts for one completed refresh. Together with partial large-frame deadline drops, these explain concrete feedback/recovery failure mechanisms; individual perceived 3–4-second stalls still lack per-frame correlation.
- No product edit, build, test rerun, deployment, restart, runtime tuning or remote Agent dispatch was performed in this diagnostic phase. Recommended scope is sequence preservation for in-epoch capture resets and verified NVENC IDR semantics, with focused regressions only. See the [diagnostic handoff](../testing/desktop-latency-quality-investigation-20260915.md); local evidence `EV-X00-T20-LOWFPS-20260920-DE`.

## 2026-09-20 — Authorized Host replacement (X00-T20)

- At the operator's immediate-replacement request, verified the existing 337-file candidate against its recorded hashes and prior build/test evidence, then performed the independent full-directory upgrade. The complete old bundle remains available for rollback, and launch arguments are unchanged. No build, automated test suite, GitHub operation or configuration tuning was repeated.
- The new Host started successfully, reconnected with the peer and resumed real media transmission. This establishes deployment and reconnection, not long-session field effectiveness or incident closure. Raw identity, launch details and deployment receipts remain local under opaque evidence reference `EV-X00-T20-HOST-REPLACEMENT-20260920`.
- Post-reconnection periodic runtime counters over 20.005 seconds show capture +1219 (60.93 FPS), encode +78 (3.90 FPS), transmit +79 (3.95 FPS), and zero additional capture/encode/transmit failures or synthetic frames. Media queue was about 2 ms in the sampled timing summaries. Throughput remains low: deployment succeeded, but neither restored smoothness nor long-session effectiveness is established. No further runtime tuning was performed.

## 2026-09-20 — Long-session false media queue (X00-T20, local fix)

- Same-instance Host evidence worsened from about 6.3 FPS / pacing 1045→888 kbps to 1 FPS / 400 kbps while capture remained near 60 FPS, media loss and DataChannel buffering stayed zero, and RTT queue stayed mostly 2–6 ms. The media relative-transit estimate reached 127–130 ms and repeatedly caused backoff, preventing recovery. This confirms the throttling decision path; it does not prove every part of the queue estimate is physical network delay or uniquely measure oscillator skew.
- Correct the lifetime-relative-transit baseline for a robust, bounded clock-rate estimate from ten-second lower-envelope samples. Do not reset queue periodically, raise congestion thresholds, change picture quality/GOP, grow frame queues, or disable independent RTT/loss/backpressure signals. Added local diagnostic clock-rate fields are optional text fields, not a wire/capability change. Local validation and running-Host rollout are separate checkpoints; the live incident is not yet closed.
- One affected 15-case adaptation/timing run and the media-transport CTest passed. At +30 ppm over 160 virtual minutes, the same deterministic queue metric changes from the previously characterized 287–288 ms to 2 ms; ±100 ppm remains below 20 ms. Sustained 25/80/250 ms delay steps, recovered queue, quantized periodic bursts, replay/old revisions and epoch reset remain covered. This is algorithm evidence, not measured post-deployment FPS. The implementation keeps 13 samples, fits ten staggered slopes once per ten seconds and adds no packet-path allocation or frame queue.
- Debug main build passed through `build.ps1 -NoPublish`; only existing getenv deprecation warnings. Initial sandbox scheduling stalled before compiler launch and was stopped; the normal-user attempt exposed an installed-toolchain mismatch, resolved by a process-local VS2022 environment matching the existing CMake compiler. No product/build configuration was changed for those environment issues. No GitHub operation, running-Host replacement/restart or full test matrix was performed at this checkpoint.

## 2026-09-20 — Periodic keyframe delivery stall (X00-T20)

- The operator reports a pause about once per second after the Controller switch. A 52.33-second GUI trace had no overflow: maximum heartbeat gap 52.959 ms, dispatch wait 32.127 ms and presentation call 0.360 ms, while decoded-frame notifications already contained periodic roughly half-second gaps. This separates the current symptom from the historical GUI event-loop stall; cumulative startup maxima are not the current baseline.
- Subsequent passive, read-only sampling of the local encoded-frame shared-memory headers observed all 183 new frames over 20 seconds, with no writer busy drops. All 18 gaps above 300 ms ended with a keyframe, exactly ten frame IDs apart: recurrence averaged 1.103 seconds, gaps ranged 497.8–545.6 ms and keyframes contained 108,654–109,925 payload bytes. The data locates the recurring delay before local decode, in the Host encoding/send/transport/reassembly chain. Large-keyframe pacing or transport buildup is the leading interpretation, not a measured Host-stage root cause.
- The separate 30-second video/navigation sample did not phase-lock stalls to thumbnail updates. The synchronous once-per-second CPU-readback/JPEG path remains a static performance risk, not the demonstrated cause of this incident. Neither Controller sender-pacer zero values nor subtraction of unsynchronized endpoint clocks establishes Host queue health or absolute latency.
- Runtime source/configuration, connection, processes and media settings are unchanged. Diagnostic tracing has been exported and stopped; generated local receipts retain the detailed measurements. No new build or CTest was needed for this diagnosis-only checkpoint; the preceding standard Debug build/publication remains the verified artifact baseline. The next step is endpoint-owned attribution of keyframe encode duration and sender pacing/transport wait against these gaps before choosing a minimal repair.

## 2026-09-20 — UPnP external endpoint publication (X00-T19)

- A subsequent explicit Controller restart switched the running GUI/runtime to the published `f5c9bfe` Debug bundle with matching executable identity. Original session, role, network binding, ICE servers and UDP 55000 were preserved; the previous complete candidate remains available. The new runtime recovered DHT/ICE and entered `streaming`, with two remote srflx candidates and nonzero peer requests/responses. A five-second sample advanced GUI decoding by 25 and presentation by 24, with zero synthetic frames, decode failures or presentation failures. This is actual post-switch Controller receive/display-counter evidence, not a new full test matrix or proof of a particular selected remote port.
- Controller-side diagnosis independently matched the current remote candidate identities and confirmed both advertised UDP ports were 55000. A single authenticated Binding probe to the same peer's router-assigned UDP 55001 received a successful response from 55001 with valid message integrity. It used a separate temporary source socket and did not redirect the existing GUI connection. Exact addresses, credentials and machine-specific receipts stay in local generated reports. The Controller checkout subsequently synchronized `f5c9bfe` while preserving its pre-existing launcher/documentation changes. Standard Debug build/publication passed; all five focused candidate/SDP/native mapped-port cases passed with zero skips. All 20 published native EXE/DLL files matched their build outputs, required Qt/terminal/maintenance files were present, and the published `--help` entry exited 0 with expected usage. The complete previous Debug directory was retained. This follow-up did not restart the active Controller, publish a GitHub Release or repeat physical desktop acceptance.
- The operator reports successful peer ICE probing of the router-assigned external port, while the exchanged candidate used the configured internal port. The local mapping receipt confirms these ports differ; this identifies the missing mapping-to-candidate path, not a new protocol-version requirement.
- Local interface change: carry the successful UPnP internal/external UDP endpoint through `IceGatheringConfig`. The net wrapper publishes an additional standard server-reflexive candidate for the matching host socket, in both trickle candidates and candidate-bearing SDP. Native host/STUN/TURN candidates and authentication are unchanged; no wire schema, capability, or peer-version change is required. Per-generation callbacks capture the mapping, and only a validated matching IPv4 UDP base qualifies.
- One Debug main build passed through `build.ps1 -NoPublish`. Four focused candidate/SDP cases and one native mapped-port ICE/control-message case passed, with zero skips. The initial parser case failed because its test-only native runtime had not initialized Winsock; after adding that fixture initialization, only the failed case was rebuilt/rechecked. The native case then passed on its first run, through an external-port-changing local forwarder with the peer using no new mapping configuration. This is native local interoperability evidence, not a completed old-release matrix or physical peer receipt.
- Authorized delivery is commit/push plus a separate staged local Host deployment/restart, retaining the old complete runtime. Running artifact identity and live connection outcomes are recorded under the local reports directory. No full suite, GitHub Release, peer upgrade, router-rule deletion or unrelated network changes were performed by the code/validation phase.

## 2026-09-20 — Controller floating task workspace (M10-T03)

- Replaced the fixed Agent sidebar, animated navigation container, terminal splitter and bottom control/file strips with an edge-to-edge remote canvas. A separate local workspace component owns four frameless native task windows, a draggable translucent wrapping button bar, relative geometry persistence, client-bound clamping and hide/restore behavior. Button toggles and window close share one hide-only state; minimization never overwrites the user's saved visibility.
- Kept one content instance per task and existing session ownership. Controller Agent messages no longer resize their outer task window; Host's independent Agent window still does. Terminal initializes at first show and preserves Shell/output lifecycle while hidden. File selection, remote browsing, paginated results and clipboard copies are embedded nonmodal pages; hiding does not cancel transmission. No protocol/capability or remote execution interface changed.
- Tool interactions suspend remote input using the existing state-release and independent gates. Focus automatically returning from a hidden tool cannot resume forwarding; an explicit canvas click is required. Owned-tool activation also raises the button bar for native WebView2 content. The renderer and online viewport contract are unchanged.
- Debug build through `build.ps1 -NoPublish` succeeded after sandbox cache-write recovery and two compile corrections. Updated obsolete layout assertions and added focused lifecycle, geometry, persistence, toolbar wrapping, input isolation and unchanged-canvas/viewport tests. Fourteen distinct unattended cases passed: thirteen initial UI cases had one old Host-resize fixture fail because its initial coalesced layout was not settled; that fixture was fixed and its isolated recheck passed. The terminal case passed separately. After the final native-activation stacking correction, the two directly affected focus/toolbar cases also passed their focused recheck. Preserve the initial failure and recheck receipts rather than presenting the initial batch as all-pass.
- Unaffected functional, wire and transport evidence was reused. No full test sweep, cross-LAN repeat, real keyboard injection, peer operation, running-directory publication, session restart or GitHub operation was performed. Visual layering/readability and actual D3D11/WebView2 desktop interactions remain optional developer evaluation, not automatic or release gates. The published v0.1.3 package is unchanged.

## 2026-09-20 — Combined v0.1.3 published

- The operator selected current main as v0.1.3 Developer Preview, superseding the separate v0.1.2-first plan. Unified CMake/vcpkg versions, bilingual README/download references and release notes; corrected the public terminal/current-user permission boundary and preserved supported-version common-capability compatibility.
- Serial Release and Debug NoPublish builds succeeded. Release publishing initially hit an inherited Windows PowerShell module-path failure at Get-FileHash; the same build/publication entrypoint completed under PowerShell 7. The existing Debug GUI/runtime were not replaced or restarted.
- Reuse prior functional and local endpoint/mixed-version evidence because runtime C++ is unchanged. The latest reorganized 24-suite automatic result had zero failures/skips; runner self-regression and script syntax passed. A redundant 110-suite Release attempt was stopped on the operator's instruction not to repeat tests. Before cancellation four DHT listener cases failed with TCP bind 10013; this incomplete sweep is retained, not relabeled passed. Only its owned CTest/test child were terminated.
- The release helper verified SHA256, extracted required files and command startup on the exact ZIP before creating a tag/uploading. [v0.1.3](https://github.com/70565912/RedClawDesktop/releases/tag/v0.1.3) was published from `d7b022e` with 334 files and a 358,508,278-byte ZIP; its SHA256 is `49c992cdca5d808c9cd22fb48176ea397312225fbbf419f681f5898e96fcf7d2`. GitHub asset digest and tag commit match local provenance, and [published-package Windows smoke](https://github.com/70565912/RedClawDesktop/actions/runs/35495853502) passed. No additional functional/manual/physical-peer run was performed. Documentation-only completion does not rebuild or retag the immutable release.

## 2026-09-20 — Manual evaluations removed from automation and release gates

- Applied the operator's explicit policy: cases that cannot finish unattended are outside both automatic sequences and version-release requirements. The developer decides which manual checks to perform during development. No post-run physical-key/paste/UAC/account/visual/peer task is demanded to close an automatic result; unperformed coverage is not advertised as passed.
- CTest command filters exclude twelve native/diagnostic cases in six mixed executables while retaining their automatic tests. Three real-desktop probe executables remain available as explicit build targets but are absent from CTest and default/automatic build aggregates. Direct CTest and the focused runner now share this selection, instead of executing manual items just to report skips. Product permissions, input filtering and compatibility behavior are untouched.
- The affected workspace/Agent/UI/media targets built successfully, and all 24 automatic CTest executables passed with zero case skips. The earlier ten-suite run with four native skips remains historical evidence, not a current pending gate. Runner regression now asserts manual exclusions; unexpected skips in the automatic set fail as test/environment errors, not waiting-for-human results.
- Updated the matrix, execution protocol, state/kanban and related runbooks to remove manual version-release prerequisites. Optional native-fixture automation is a future improvement, not unfinished acceptance or a required next action. No main-app build/publication, remote action, deployment or restart occurred.

## 2026-09-20 — X00-T17 local-first automated validation

- Replaced the obsolete offline-signal/full-remote matrix with one execution-location contract: local isolated/native correctness, local Host/Controller integration and supported-version rolling compatibility, then a minimal physical public-DHT/ICE/channel checkpoint only where the network boundary matters. File, clipboard, terminal, UI, Agent parser and upgrade-fault combinations no longer require redundant remote repetitions. Safety, true capture and supported-version negotiation constraints are unchanged.
- Added one focused runner over 31 existing CTest targets in seven selectable areas. It deduplicates targets, uses the serialized test-only build guard, bounds serial tests, fingerprints executed binaries and writes JSON/JUnit plus GoogleTest case XML. Missing/zero/duplicate tests, inherited filtered/sharded runs and internal skips cannot silently become a pass. It does not publish/restart the application, enable opt-in native input/real Providers, contact the peer or auto-retry failures.
- Script regression covers selection, registered target names, failed/missing/duplicate/skip interpretation and real CLI blocked receipts. The five-suite core build succeeded; four suites passed in the sandbox and the broker's metadata-restart case failed, then all five passed in the normal-user environment without a rebuild. The workspace build and all ten CTest executables succeeded, but four native clipboard/focus cases skipped; the runner correctly reported `partial`, not full acceptance. Original receipts remain separate. No Release/full-suite or new physical acceptance is claimed.
- Reclassified the already visible remote click/digit as valid application-consumption evidence; additional peer application readback is diagnostic, not a duplicate gate. Physical-keyboard Hook capture and native application paste remain local gaps on X00-T18, not new remote-operator chores. The initial target's disappearance and historical latency/quality incident remain unresolved. Cleaned conflicting historical resume instructions while retaining their evidence here.
- Updated execution/test guidance, workspace contract and related runbooks to share this routing. Current remote workspace follow-up is actual capability negotiation plus a small transfer/terminal round trip, not a full physical matrix. Genuine consent, UAC, physical keyboard and topology actions remain explicit manual boundaries. Full end-to-end automation of those fixtures is not claimed.
- Windows PowerShell 5.1 and PowerShell 7 runner regressions, syntax checks and the modified-document link check passed. PSScriptAnalyzer is unavailable locally; no installation was attempted. The old bridge same-SHA condition and broken testing links were removed rather than retained as conflicting requirements.

## 2026-09-20 — Physical temporary-target input response

- A GUI-originated click through the actual Controller changed the isolated peer target's mouse counter from zero to one. After focusing its temporary edit box, one digit sent through the existing guarded QA key interface became visible in that box. Controller counters recorded one key-down/up pair and two mouse-down/up pairs, including the focus click.
- A Computer Use key attempt produced no key send: the Windows hook explicitly filters `LLKHF_INJECTED`. The QA path retains active-control, foreground and queue checks but does not qualify the physical-keyboard capture entry. No filter, permission or security setting was modified.
- The same remote Agent task returned a partial readback: six Host received batches matched the six Controller batches at the test checkpoint, with twelve injected events, zero rejected batches, an empty queue and last-applied sequence 2017. Separate validated/consumed counters were unavailable. Controller-visible target response and peer-reported API injection are distinct evidence; neither resolves the historical intermittent incident.
- At the later readback the temporary target no longer existed, also confirmed in the Controller image, so the peer could not independently reread the application. Withdrawn the pending physical-key test and paused control while retaining video. The shared source reclaims an owned Provider job before starting another turn, which could explain loss of a descendant fixture, but that fixture's lifetime relationship is not confirmed. Further validation needs a stable isolated target, not removal of process-containment rules.
- No product code, build, Git operation, peer restart or deployment occurred. Runtime documents and local input/peer-result evidence were updated; `git diff --check` passed. This extends the previously built Debug checkpoint, not a new build or full-suite run.

## 2026-09-20 — Current Debug Controller physical cross-LAN checkpoint

- Built `88a3803` through the standard Debug entrypoint and published the complete app-local runtime, including the verified fixed WebView2 runtime. Recovered a mixed VS2022 compiler/VS18 header environment using a process-scoped VS2022 developer environment; no source or persistent toolchain configuration was changed. Preserved the previous complete runtime for rollback.
- All 11 focused capture/adaptation/protocol/transfer/terminal/Agent/UI/input suites passed. Three initial sandbox-sensitive failures passed under the normal user; one opt-in diagnostic-paint case was skipped. This is not a new full-CTest or Release qualification.
- Started the real Controller with Remote Agent enabled and connected to the existing peer Host via public DHT/ICE. The playback window showed the remote desktop; H.264 reception and D3D11 decode/presentation advanced with zero GUI decode/presentation failures. Control activation received a successful Host acknowledgment; no automatic keyboard or mouse injection was used to claim application delivery.
- Historical Agent resume requests were rejected without overwriting retained task state. Static inspection shows that restored broker metadata does not restore the Cursor provider session; this explains a possible resume boundary, not a confirmed peer rejection reason. A fresh task on the same Cursor executor reached whole-turn approval, then paused on approval timeout. After explicit operator approval, a new task-scoped request was accepted and the same task completed actual Provider execution and returned a redacted result. Keep endpoint-reported diagnostics separate from independent Controller evidence; the first-stage completion does not establish actual keyboard/mouse delivery, physical file/clipboard/terminal acceptance, mixed-version behavior, performance improvement or incident completion.
- Exported the local evidence manifest and left the Controller connected for the operator's input test. No peer deployment/restart, Git synchronization or product-code change occurred in this checkpoint.
- The approved peer diagnostic reported build `88a3803`, ordinary non-secure desktop input eligibility, and a 10.67-second WGC sample with capture/encode/transmit increments of 38/37/37 and zero synthetic/failure increments. It attributed 1680x1050 to 1224x766 scaling to Controller viewport fitting; roughly 3.5 FPS versus a 30 FPS target remains an observation with a capture-side hypothesis, not a resolved bottleneck. The Controller visually confirmed the isolated temporary input target and retained zero input-event counts. Optional terminal/file/clipboard versions were inferred from source rather than directly observed negotiation, so those physical gates remain open. The receipt's Cursor-readiness wording conflicts with the actual accepted Cursor turn and needs endpoint clarification before using it as a readiness conclusion.

## 2026-09-16 — Connected maintenance and remote-workspace qualification

- Complete fixed-terminal bundles now use one shared, bounded handoff protocol: exact independent-worker identity and arguments, advancing preflight stages, immutable plan acknowledgment, full-directory release and startup-only rollback. Legacy and portable upgrade fixtures passed, including delayed file release, occupied files, corrupt bundles, parent/Host exit, slow startup and recovery failure. Actual Controller and Host full-directory upgrades completed; an earlier WebView2 file-release failure safely restored the previous bundle. Network discovery delay is recorded separately and does not trigger rollback.
- A command entered in the actual remote terminal restarted Host through the independent worker. The Controller survived, reconnected, displayed a fresh Shell without its old variable, and observed the final `completed` receipt. A subsequent 20-second interval advanced real capture, encode, send, receive, decode and presentation, with zero synthetic/decode/presentation failures. Repeating the original operation ID from a later installed Host context returned its original receipt without changing either process identity. Operation lookup validates the protected launch history; interrupted workers remain observable without rewriting or replaying their evidence.
- Restarted Hosts can begin again at lower frame IDs. Local decode generations and session-owned display watermarks now reject retained/in-flight frames and stale telemetry. Three native receiver/D3D11 cases and all 19 rebuilt progress/context/transfer/input cases passed. Ordinary Controller reconnect now preserves the terminal lifecycle; explicit desktop End still cleans up the Shell. Reopening a GUI pipe resends only the bounded unacknowledged output chunk for deduplication, recovering a lost output ACK without replaying commands. All five native terminal tests passed, including real keyboard input, both reconnect paths, a fresh Shell after End and paused input.
- Final serial Debug/Release main builds and the rebuilt online Debug two-window gate passed. An actual Controller runtime reconnect retained its Host Shell variable with the terminal continuously expanded; its new PID survived the old stop deadline. All test targets were explicitly rebuilt after the final reconnect repair. The final full CTest run completed with 112 passed, one DDA environment skip and zero failures. Desktop Duplication returned E_ACCESSDENIED on the local display. The earlier ICE answer-after-expiry emulator-error assertion failed once, passed alone, and passed again in the final full run; that initial failure remains in the local evidence. Eight embedded cases skipped for opt-in Provider/terminal/paint, local QSV support or clipboard-window-station permissions. The separate opt-in terminal suite passed all five cases. All four local online DHT/RTC version combinations with published v0.1.0/v0.1.1 passed real-video gates; older Controllers/Hosts negotiate only their common capabilities.
- The Release archive now includes the fixed terminal runtime, four maintenance scripts and generated license notices. A local archive made with the actual packaging functions passed SHA256 and extracted command-entry smoke; a ZIP declaring terminal support without its runtime was rejected. Both changed release scripts have no new lint warnings; the existing New-PortableArchive ShouldProcess warning remains. The actual extracted Release GUI then passed online DHT/RTC real-video and screenshot gates. Its first cold-start attempt stayed at the local Agent readiness check with no Controller runtime; the unchanged second run started successfully after local provider discovery. That startup timing limitation remains recorded. This working-tree package exercise does not replace clean-release or post-publication acceptance.
- A matched local animation/video baseline compared the previous Debug runtime with the final candidate at native 1680 x 1050 using WGC, QSV and D3D11 playback. Across four owned GUI/runtime processes, normalized CPU was 14.86% / 14.03%, working set 567.05 / 578.67 MiB and playback 23.87 / 24.67 FPS (before / after), with zero synthetic/decode/presentation failures. The animation itself ran at 28.94 / 31.30 FPS; these single samples are baseline evidence, not a performance-improvement claim or concurrent file/terminal/Agent qualification.
- X00-T16 remains in progress. Actual application Ctrl+V, matched concurrent-workload performance, physical peer rolling upgrade and public package/release acceptance remain open. Local two-endpoint, fixture and native-window evidence is not physical cross-site acceptance. The physical peer Agent channel is still offline; no public v0.1.2 release or peer upgrade is claimed.

## 2026-09-15 — Qualification continuation and v0.1.3 local components

- Host maintenance now has an immutable current-user session context, fixed terminal restart/update commands and a portable version-2 plan for the existing independent full-directory worker. A repeated operation ID reads its receipt without replaying a stop. The five portable and five legacy isolated upgrade scenarios passed, including Agent-style Job exit; additional tests passed Host exit after handoff and restored-runtime startup after rollback. The final context/input subset passed all 17 cases. Real online terminal testing then found Windows PowerShell inheriting incompatible PowerShell 7 module paths through the native Host: module autoload failed before handoff, leaving Host and its connection running. The shell now reconstructs its module paths in a private child environment while retaining other variables. All eight terminal cases passed, including a deliberately invalid inherited module path and actual Get-Acl execution. Live maintenance/reconnect qualification is continuing; this is not a completed physical rollout.
- Received clipboard files now have negotiated version-2 list/open/cleanup actions and a real Qt selection dialog. Version-1 clipboard peers retain paste without receiving these new operations. Each batch has a generated opaque identity and a local ownership marker; fixed cleanup pins directory ancestors, removes reparse links without traversing targets, retains retryable ownership on cancellation/file occupancy, and uses the existing disk worker and both completion receipts. Pure payload staging without copied file entries is removed at completion/cancellation. Clipboard entries cannot overwrite batch ownership metadata. Four focused CTests passed. Actual window testing found native accessibility selection not setting Qt's current item; actions now follow the selected row, and all 16 focused GUI/input cases passed. A rebuilt online DHT/RTC run then verified the selected copy's folder opened in Explorer, its file hash remained unchanged, and cleanup removed the batch with a visible completion result. Both screenshots were inspected; all isolated endpoints exited normally. This used an explicitly seeded owned-copy fixture and did not change the system clipboard or establish application-paste acceptance. Native application paste and connected Host maintenance remain open.
- Clipboard transactions now advertise an optional versioned capability, prepare source snapshots/received native objects on the existing disk worker, and retain the shared transfer gate until the Host reports its one-shot paste result. Local clipboard sequence and result-journal paths are rejected at the peer boundary. The canvas withholds original/repeated Ctrl+V and cancels on a different local focus; the Host rechecks its original focus, capture, channel and input eligibility after this tick's stop/revocation processing. Empty state sync releases keys without ending the control grant. Three fresh protocol/worker/runtime CTests and all 14 focused GUI/input cases passed, followed by the complete Debug build. Native callback tests call the handler directly and do not constitute physical-key or application-paste proof. Native clipboard round trips remain isolated-window-station permission skips; received-copy actions, connected maintenance and physical acceptance remain open.
- The corresponding Release build and rebuilt online DHT/RTC native-video gate also passed. In the actual Controller window, cancelling a 256 MiB fixture after about 116 MiB restored Agent submission while preserving control intent and the independent local-focus reason; no unfinished file remained. Screenshots and dual-endpoint status were retained locally, and all four isolated GUI/runtime processes exited normally. A later native WinEvent test passed focus-away-and-back invalidation. Its first combined run exposed fixture cleanup attempting an unnecessary desktop switch after a skip; fresh-thread window ownership and restoration only after a successful switch fixed that test interference. The final run passed the two guard, one native focus and two prepared-object cases, with the three clipboard round trips still explicitly skipped.

- The final v0.1.2 approval correction passed serial Debug/Release builds and focused tests. Its subsequent full 102-target sweep had one existing UI heartbeat failure; a focused rerun passed. Desktop Duplication was then denied even under the interactive user and was explicitly skipped. Earlier successful capture probes do not establish current availability. Candidate ZIP/manifest hashing and extracted command-entry smoke passed; packaged GUI and physical gates remain open.
- The independent current-user worker took ownership, replaced the complete local Debug runtime, restarted GUI/runtime with retained settings and reported startup healthy. Desktop/Agent reconnection did not recover, so peer preparation, formal peer-first rollout and final local-Controller/peer-Host roles are still pending. Code and qualification documentation were pushed normally; no v0.1.2 tag or release was created.
- v0.1.3 work replaces covering navigation with an animated layout item, reserves usable Agent space and follows new selected-conversation output at both scroll levels. Functional regressions passed; heartbeat timing failures also reproduced on the prior UI baseline. Native update-event waits were observed, but their cause and performance impact are not yet established.
- Added an owned ConPTY session with bounded input/output queues, output backpressure, PowerShell persistence and a kill-on-close process job. Actual integration tests cover Unicode, resize, Ctrl+C, saturated output shutdown, descendant cleanup and fresh-session non-replay. The first test run exposed Windows duplicating redirected parent standard handles into the shell; explicitly requesting null standard handles repaired the ConPTY data path. All three terminal lifecycle cases then passed.
- A separate Qt/WebView2 terminal surface embeds fixed xterm assets, blocks remote resources/navigation/permissions and OSC clipboard writes, and acknowledges parsed output before accepting another chunk. Session reset has a separate acknowledgment to exclude previous-session output. The SDK/assets/runtime are pinned and the fixed runtime's Microsoft signature and hashes were verified. Actual display/Unicode/collapse/OSC isolation tests passed and a complete native window screenshot was inspected.
- The terminal now connects to the GUI/runtime owners through an owner/PID-checked bounded pipe and an optional reliable ordered terminal channel negotiated in Control v1. The Host owns ConPTY, while the Controller tracks parsed-output acknowledgments, one unacknowledged input chunk and changing input generations. Serial Debug/Release main builds passed. A real keyboard test through the production panel, local pipe and serialized wire reached PowerShell, retained the same shell and variable through collapse/reconnect, and emitted no keyboard input while paused. It exposed and repaired missing DOM focus after native WebView focus. Later visual reruns stopped at the foreground-owner guard; the input desktop was still ordinary, so those runs are neither passes nor proof of a locked session. Actual network terminal-command and mixed-version acceptance remain open.
- Online DHT dual-GUI tests passed real native-size WGC capture, transport, decoding and presentation with zero synthetic/decode/presentation failures; the optional terminal data channel opened. A first product screenshot exposed Qt retaining the collapsed terminal's splitter slot. The focused reproduction and a second rebuilt dual-GUI run verified the layout correction visually. These are local two-process endpoints, not physical peer or network terminal-command acceptance.
- Rapid channel close/reopen now forces an input-generation barrier even between owner ticks; retained Host output waits for a fresh Controller open. The local terminal transfer gate drops queued user input while continuing output/ACKs, and cannot clear an independent Host pause. Focused state, real pipe and WebView tests passed. The terminal's access-only desktop probe retains the same permission checks: one local 20-call comparison measured 175 microseconds per access-only probe versus 1629 microseconds with display enumeration; this is a probe measurement, not an end-to-end latency improvement.
- The receiver's per-file transaction streams into an owned temporary file, verifies 64-bit length and SHA256, then commits by handle. Local filesystem tests passed for binary and empty files/folders, path validation, keep-both/overwrite/skip, cancellation, checksum failure, occupied destinations and sizes above 4GB without truncating counters. Original complete files remained intact on failure. The versioned bulk envelope caps each data chunk at 48 KiB within the existing transport frame limit; batch tests round-trip real serialized data through file commit, reject repeated commits/stale identities and cancel pending work while retaining completed files. A worker-local source manifest streams scanned metadata into an exclusive delete-on-close spool; Unicode/empty folders, accurate totals and scan cancellation passed. The sender retains one unsent chunk with a 1 MiB window of unacknowledged file data. A 3 MiB sender/receiver round-trip passed actual destination hashing and bounded-window checks; blocked sends retained the exact chunk, changed source length failed explicitly, and cancellation prevented further sends. A subsequent worker/runtime checkpoint added incremental source selection, bounded independent disk threads and optional bulk-channel negotiation. Serialized two-endpoint runtime tests passed uploads, downloads, priority cancellation while bulk sends were blocked, rapid channel flaps and unsupported-capability rejection. The shared operation gate is connected to Agent submission/steering/approval, Host input and terminal input; focused regressions verified held-input release, stale-input removal and independent pause preservation. The file picker, streaming remote directory browser and progress/cancel controls now compile in the Debug main program; The real-dialog serialization and delayed-unlock UI regression passed, as did Agent submission/approval gating with retained drafts/output. Runtime tests also passed remote directory enumeration and cancellation while a completion receipt was blocked, retaining already committed files. A subsequent actual Controller-window run selected sources and destinations through the native picker and remote browser, transferred a Unicode folder in both directions through online DHT/RTC, and matched SHA256 for a 32 MiB random file, Unicode text and an empty file; the empty folder was retained. A 256 MiB download was cancelled after the window reported approximately 31.9 MiB received, leaving no partial files; the 2.6-second observed cleanup interval includes UI automation overhead. A prior attempt completed before cancellation and remains a completed transfer, not a cancellation pass. During the large-transfer interval real Host capture/encode/send and Controller receive/decode/display advanced, with zero synthetic, decode and presentation failures. This fixture did not authorize desktop input or Agent execution, so those disabled controls do not prove an active-to-paused transition. An actual network PowerShell command and Unicode output were visually confirmed after explicitly focusing the xterm input field. Progress now separates received and SHA256-verified bytes, shows the current relative path, direction and sampled receipt rate, and fixes the remote browser header contrast; serial Debug/Release builds, four transfer CTests and the focused panel test passed. Physical-peer, performance and full-workspace acceptance remain open.
- Actual network shutdown testing exposed a retained Host shell after the Controller GUI exited. The explicit End/Ended exchange now precedes runtime termination, including close-before-Ready and paused-input cases, and a new desktop identity cannot inherit the previous client's shell. Rebuilt terminal/protocol tests and the live GUI rerun passed: the shell exited before the Controller runtime, while the Host runtime remained available. Live transfer also disabled the previously enabled fixture Agent new-task action and terminal input, restoring both after cancellation. The input fixture was found to pass a CLI flag while its isolated GUI consent checkbox remained false; the test now seeds only that fixture's explicitly requested consent. The subsequent online DHT/RTC retry reached active desktop control. Its screenshot and state readback confirmed the transfer pause reason, disabled Agent new-task action and paused terminal input; cancellation removed the partial file and restored eligibility while retaining the independent local-focus reason. The first authorized run failed at Controller DHT publication before any connection, and remains a recorded signaling failure. Clipboard transactions and connected maintenance actions remain incomplete; the new remote workspace is not declared delivered.

- Per-file transfer results are recorded on the existing disk worker in a versioned owner-local journal; the GUI reads bounded pages asynchronously and shows actual keep-both destination names, skipped entries and retained completed files after cancel. The peer boundary rejects local result paths. Three freshly rebuilt transfer CTests passed, as did both real Qt panel/page cases. A new Debug main build and complete candidate deployment then passed online DHT/RTC real-video qualification, a GUI-driven download with three matching file hashes and an empty directory, and visual inspection of the five-row result dialog. The same candidate retains the verified terminal End/Ended shutdown fix. A separate clipboard latch now tests focus-return detection, revocation, stale receipts and at-most-once submission; the native snapshot/prepared-payload adapter subsequently compiled, with two received-payload filesystem tests passing for actual Windows object allocation, SHA256 mismatch, manifest version, duplicate formats and cancellation. Streamed clipboard root aliases also passed while preserving basenames, source files and empty directories. The three isolated-window-station clipboard round-trip tests skipped because window station creation was denied, including in the native execution environment; they are not successful system clipboard or application-paste evidence. Connected snapshot/transfer/paste and its UI remain unimplemented. The result-journal/terminal-End checkpoint also passed its serial Release main build before the later clipboard adapter changes.

## 2026-09-15 — M01-T03 qualification and Agent approval continuity

- Rebuilt Debug and Release serially against the latest main baseline; all 102 CTests passed in 908 seconds. Both directions against the original public v0.1.0 Release binary displayed native-size real video with zero synthetic, decode and presentation failures. The first old-Host attempt failed before DHT publication; a fresh attempt passed, so that failure is retained as signaling evidence rather than hidden as a compatibility pass.
- An isolated non-input desktop reproduced capture denial before the first frame: Host paused without trying alternate backends, but the Controller recovery window stayed hidden. Present the paused workspace on negotiated capture status without marking a frame ready. The same fixture then displayed the pause message and retry action with control disabled. It never switches or locks the user's input desktop.
- Physical preparation through the existing Agent channel exposed pending approvals disappearing after background Provider output reported a running state. Preserve the pending request in the broker and keep its UI reachable with older peers; only an explicit decision, pause or turn termination releases it. Broker/UI regressions failed before the correction; the rebuilt Debug program and all four focused broker/UI/capture/protocol CTests then passed. Approval scope and the timeout policy are unchanged; final Release/full-suite and physical qualification follow.
- Remote preparation remains incomplete: approval timeouts paused the peer task. No peer deployment, role switch or restart has occurred in this checkpoint. The approved remote-workspace extension is recorded separately for v0.1.3, after v0.1.2 qualification and release.

## 2026-09-15 — X00-T14 Agent synchronization rejection handling

- Follow-up authorization now covers committing/pushing the scoped changes, rebuilding and restarting this machine as Host. Added the [diagnostic handoff](../testing/desktop-latency-quality-investigation-20260915.md) with established sender-throttling evidence, the unproven queue-estimator trigger, a separate static-quality investigation, immediate healthy baselines and recurrence-before-recovery evidence. Corrected the current task board's superseded missing-key blocker from the later peer result. Deployment/standby and physical incident acceptance remain separate gates.

- Read-only local evidence identified repeated remote `request_rejected / not authorized` responses, not successful task snapshots. The Controller had persisted the executor's placeholder paused state and kept retrying synchronization; a pending-sync UI badge also hid the authorization status. No remote permission was changed or bypassed.
- Request-level rejections no longer overwrite durable task state, approvals or ACKs. Complete authorization-denial capabilities stop automatic sync retries without clearing pending work. Authorization recovery and a new epoch require fresh synchronization of nonterminal tasks; in-flight snapshots during denial cannot waive that recovery gate. Validate epoch envelopes before changing state and preserve replay-guard consistency when persistence fails. Offline/unauthorized UI states take precedence over syncing.
- Four focused regressions were observed failing before their fixes. Final serial `build.ps1 -Configuration Debug -SkipConfigure -NoPublish -Target redclaw_desktop` and five focused Agent/GUI CTests passed: 22 coordination cases, 7 peer-session cases, 18 broker cases, 5 local-control cases and 94 UI cases passed; the existing opt-in diagnostic-paint replay case was skipped. Static peer review covered authorization recovery and epoch failure boundaries.
- No publication, replacement, restart, GitHub operation, remote command, authorization-record reset or live-journal repair occurred. The running executable and GUI/runtime identities remained unchanged. The candidate includes the pre-existing worktree changes. Physical acceptance still requires separately authorized local deployment and endpoint-owned authorization; this does not complete the picture-quality incident. A general request rejection can still leave a locally queued request pending in the UI until explicit synchronization; its diagnostic is retained in the normal log, not promoted to a provider task result.

## 2026-09-15 — X00-T13 remote Agent result contract

- Added an instruction-only repository skill and mandatory AGENTS routing: the Controller provides authorized goals and result criteria, while the endpoint Agent chooses methods, handles local permission decisions and executes checks locally. Only necessary redacted outcomes cross the collaboration boundary, not raw process, account, permission or command details.
- Separate archive extraction/validation from live deployment dependencies; a blocked stage does not invalidate independently completed work. Preserve local approvals, secret handoff, single-executor ownership and task-versus-incident completion distinctions. Delegation never bypasses an explicit denial or grants new authority.
- Skill format and navigation checks passed. An independent six-scenario paper exercise covered missing keys, diagnostic-only authorization, raw-environment requests, unavailable tools during an authorized deployment, prohibited cross-agent delegation and monitor closeout; feedback clarified operation-specific denial scope and overall completion criteria. No product code, runtime settings, build, restart, remote execution or credential publication was performed. The skill defines the new collaboration policy; distribution and adoption on another machine are not implied by local creation.

## 2026-09-15 — M08-T03 Host media timestamp-pair diagnostics

- Interface scope: expose the estimator's latest applied send/receive timestamp pair, its feedback/packet/frame/revision identity, anchors and EWMA/minimum through a typed read-only diagnostic snapshot. Keep a single fixed-size sample under the existing estimator lock; format it only in the existing periodic Host log path. No payload, input content, credentials, new wire fields or congestion-policy changes.
- Seven new deterministic C++ tests cover fixed delay/offset, positive/negative clock-rate skew, actual delay steps, sample retention/reset/rejected feedback and single-line snapshot bounds. With no growing network delay, a +30 ppm receiver clock produces a 287 ms queue estimate after 160 virtual minutes and triggers severe congestion pressure; zero and -30 ppm produce zero queue. This characterizes the unchanged estimator, not measured endpoint skew or an incident fix.
- The serial offline Debug desktop build and all three focused CTests (stream adaptation, video frame transport, transport recovery) passed. The adaptation executable passed all 13 cases. The desktop build retains existing `getenv` deprecation warnings. No Release/full-suite or physical deployment validation is claimed.
- Publication, replacement, restart/reconnection and remote Agent execution were excluded. Active GUI/runtime identities and the published executable hash remained unchanged. The build includes the pre-existing worktree changes; field deployment/package qualification and real timestamp-pair collection remain pending on `M08-T03`.
- Deployment follow-up: prepared a separate complete 59-file Debug candidate with matching compiler/UCRT dependencies, validated its file manifest/imports and passed a hidden `--help` command-entry probe with development directories removed from PATH. The remote read-only identity/version/transfer preflight hit an Agent execution-approval timeout and paused. No remote files were transferred and no upgrade worker, stop, replacement or reconnect occurred; operator-assisted preflight approval and remote qualification remain required.
- Preflight reissue: the remote task completed with partial results, not a passed deployment gate. The peer reports a clean checkout at the same HEAD, missing timing diagnostics and three upgrade scripts, denied process-identity access, and no qualified remote candidate or established approved transfer route. Recovered the complete already-observed report by bounded evidence-tail collection without rerunning commands. Local live process identities and executable/candidate hashes remained unchanged; no transfer, worker, replacement, elevated operation or new build occurred. Candidate scope and authorized identity/transfer qualification remain required.
- Authorized transfer follow-up: restored the private mailbox history without changing historical message bodies, added a fresh scope/request and append-only receipts, and kept normal Agent as the execution authority. The full previously validated candidate plus fixed upgrade scripts passed manifest, AES/header-encrypted archive and wrong-password checks; the private draft Release returned the matching SHA-256 digest. The password was delivered only through authenticated Agent. Sender download readback did not complete. The peer reports its current identity query still returns access denied, so it did not download, decrypt, overwrite scripts, start a worker or replace/restart Host. Local Controller identity/hash remained unchanged; no new build, elevation, input operation, live timing evidence or incident fix is claimed.

- Manual-coordination follow-up: published a fresh staged private-mailbox request for the user-notified peer local Agent, rebased on and preserved a concurrent peer receipt. That receipt reports newly approved Host identity and an actual hash-matching encrypted download; key access and an existing task-writer conflict remain unresolved, not a passed upgrade. Restored the existing local five-minute heartbeat on this conversation and requested the peer monitor's exact identity; genuine joint-debug completion requires a scoped peer stop request and verified receipt before pausing the local monitor. All five published mailbox documents passed exact readback. No new build, source/config edit, runtime operation, credential publication or additional built-in Remote Agent dispatch occurred in this stage.

- Same-instance evidence follow-up: the peer's retained Host log shows 113 captures, 15 encodes and seven transmissions over 30 seconds, with severe transport-queue pressure, a 400 kbps pacer and four frame-budget rejections. Nearby Controller reception/presentation is comparably sparse; clocks are not calibrated for packet-loss or one-way-delay arithmetic. Independently checked source distinguishes the 1785 kbps encoder quality floor from pacing, local pacing drops from transmit failures, and the 5-second ordinary / 10-second recovery budgets (correcting an earlier universal recovery-budget interpretation). The Host-local executor now reports no active competing update task; separate key handoff and real paired timestamps remain pending. The mailbox follow-up requests only existing rejection-event details. No new product change, build, restart or input action occurred, and root cause/resolution is not claimed.

## 2026-09-14 — M01-T03 capture recovery implementation (in progress)

- Interface work: capture now exposes typed failure stage/category/HRESULT, local desktop context, availability and generation, plus an explicit retry request. Control v1 negotiates optional capture-status v1 fields on existing capability/source-activity messages; cursor pixels remain in the existing video stream.
- Recovery must invalidate retained frames, request a fresh keyframe and pause/release Host input independently of peer capabilities. A supported receiver's presentation acknowledgment is the recovery completion boundary. Local Controller cursor appearance remains the system default.
- Serial Debug/Release builds and all 102 release-checkpoint CTests passed. Twenty focused recovery/cursor checks passed; native encoder device recreation is an explicit local-driver skip because QSV device derivation failed. Real DDA/WGC/GDI probes had zero capture failures. New/new and both pre-upgrade Debug mixed directions passed real-video checks; a mixed DHT Host restart recovered. Both directions with the original published v0.1.1 Release ZIP passed after ZIP/EXE hash verification.
- Added full-directory upgrade validation with owner-only plans, exact process identity, graceful stop, rollback, and a current-user independent worker; isolated corruption, identity, occupied-file, startup failure and Agent job-exit cases passed. The actual local GUI/runtime family passed plan-only preflight without being stopped. A matched native-size motion-scene comparison measured old/new total CPU 17.71%/17.53% and working set 480.9/490.0 MiB; no speedup claim is made. Baseline/worktree/runtime backups remain local. Physical rollout, v0.1.0 compatibility and publication are still in progress.

## 2026-09-14 — DDA access-denied diagnosis

- Reconstructed a local Host transition from successful Desktop Duplication capture to repeated frame-acquisition failures, followed by a capture-session restart that returned `E_ACCESSDENIED` and selected WGC. The surviving acquisition log omits the HRESULT, so the initiating desktop/driver event remains unproven.
- Rebuilt the independent DDA PoC and capture runner. The PoC and three-second probes on both display outputs succeeded in the current ordinary user desktop, with no backend fallback or capture failures. Current desktop/token checks do not establish the historical failure context; persistent hardware incapability or a universal administrator requirement is not supported by these results.
- Kept the active Controller connected and left product code unchanged. Track preservation of acquisition HRESULT and capture-thread desktop context, followed by appropriate DDA recovery validation, on `M01-T03`.

## 2026-09-14 — Controller startup with multiple physical IPv4 addresses

- Reproduced the Debug Controller launcher's false "No active physical Ethernet/Wi-Fi IPv4 default route" failure when one physical adapter has multiple eligible IPv4 addresses. The launcher now uses Windows route selection to identify the source address for that adapter's default gateway, retaining the existing physical-adapter and ambiguity checks.
- Built and published Debug through the standard build entry point, verified matching build/published executable hashes, and launched the actual Controller command entry against the waiting remote Host. Public-DHT pairing completed and the Controller displayed the remote desktop with D3D11 H.264 decoding.
- A roughly 69-second Controller sample added 451 received/reassembled frames, 421 GUI decodes, and 417 presentations with zero GUI decode/presentation failures. The remote Host snapshot independently showed nonzero capture, encode, and transmit rates with zero synthetic-frame rate. Saved a visual screenshot and exported the evidence manifest locally.
- PowerShell parser validation and diff whitespace checks passed. PSScriptAnalyzer was unavailable locally; no full CTest, new Provider login, TURN, or long-duration acceptance is claimed by this operational checkpoint.

## 2026-09-14 — Agent conversation panel content-sized window

- Connected the virtualized conversation viewport's measured content height to its top-level window size hint. The Agent panel now grows as remote replies and expanded activity details are measured, coalesces streaming resize requests, and clamps the result to the active screen so long histories remain scrollable.
- Added UI regressions for window growth/shrink and expanded wrapped activity details. The shared Debug build and full UI connection-flow suite passed after using a consistent MSVC 14.44 compiler/header environment; seven related suites passed together. The latest Release publication and physical two-machine UI validation remain pending.

## 2026-09-14 — Opt-in input delivery diagnostics

- Extend the existing bounded input-stage recorder with category counts, explicit mapping/rejection stages, and native SendInput return/context observations; no key values or text are retained. Add a Debug-only diagnostic launch option, independent of synthetic Agent fixtures.
- Add a passive Host diagnostic window to distinguish OS native-message delivery from API insertion. It observes only its own window, does not install global hooks or grant input authority, and leaves normal Control/Agent wire formats unchanged.
- Debug desktop build with the online DHT backend and all seven focused suites passed: runtime options/receipts, input session, UI connection flow, Agent provider, coordination, broker and isolation. Physical deployment and target-window delivery evidence remain pending; no latency or UAC-control improvement is claimed.

## 2026-09-14 — Agent approval identity and bounded result replay

- Scoped durable approval lookup to task plus request ID. A provider counter reused after Host restart must not inherit another task's accepted/rejected approval; the existing journal schema is retained and replayed with the corrected key. Same-task duplicate decisions still fail closed; ordinary request uniqueness and cross-task replacement semantics are preserved.
- Changed transcript synchronization to cursor-based bounded batches. Transport queue pressure requests replay from the durable acknowledgment instead of reporting retained text as permanently lost; real provider/cache loss remains explicit. Added a long-history/small-queue regression.
- Applied bounded backpressure to dedicated provider pipe readers, waking blocked readers before shutdown. Preserved Codex command-output payloads, reported failed/interrupted turn completion correctly, and released failed process/turn ownership so an explicit retry can resume the registered thread.
- Retained the related Cursor first-approval retry repair: an unlaunched registered task starts only after fresh approval, while an already launched task must resume its registered chat.
- Agent v1 wire fields and security gates are unchanged. Debug build/publication and packaged startup passed, as did the six focused Agent/IPC test suites (including the 60-second isolation gate). Physical mixed-version runtime validation is tracked on `X00-T10`; remote UAC remains a field-evidence gate, not a conclusion from input counters.
- Physical upgraded-Controller/old-Host reconnection and task-scoped approval reuse succeeded. A read-only remote probe and Agent report readback found Default input desktop, no consent/LogonUI, and matching Host/probe sessions at sampling time; this does not reconstruct the earlier input failure. Host-side result-flood deployment/acceptance remains open.

## 2026-09-14 — Cursor and Codex conversation output parsing

- Parsed Cursor `stream-json` assistant content from nested `message.content`, enabled partial output, surfaced bounded tool-call summaries, and used the terminal `result` as a fallback when no assistant message was emitted. Duplicate buffered/final assistant output is filtered per turn.
- Extended Codex app-server handling beyond `params.delta` to nested `item` content and completion text, while keeping tool item details visible as bounded activity summaries. No Agent wire schema change was required.
- Added Provider regressions for nested assistant text, tool details, completion fallback, Cursor result de-duplication, and the existing approval/resume path. The full Agent Provider suite passed 18 tests with one opt-in real-provider readiness test skipped; all 17 conversation-panel tests passed.
- Release rebuild and packaged `--help` startup validation remain required after this parser change; physical two-machine Cursor/Codex validation remains open.

## 2026-09-14 — Agent message layout and Cursor approval retry

- Removed the blue background band around outgoing messages while retaining a single 1 px outline. Expanding or collapsing remote activity now invalidates that row's cached height, remeasures wrapped details, and repositions subsequent messages.
- Fixed follow-up submission after the first Cursor approval is rejected, expires, or is interrupted: register the task's model and workspace before approval, distinguish an unlaunched task from an existing chat, and require fresh approval before starting the new instruction. Started conversations still resume their recorded chat; unknown tasks and launched tasks without a chat ID remain rejected. No wire schema or capability changes were required.
- Reproduced both the stale expansion height and the exact missing-chat error with regression tests before fixing them. After the fix, 17 conversation-panel tests, 15 Provider tests, and five broker approval tests passed; the opt-in real Provider readiness test was skipped. Native Qt screenshot validation confirmed the single border and fully visible expanded Chinese/English details.
- Built and published the Release program through `build.ps1`, verified the published executable matches the build hash, and passed its `--help` startup check. The existing Debug instances were left running. Real two-machine Cursor conversation validation remains open.

## 2026-09-14 — network-exit selection analysis

- Documented the current System default and explicit-adapter binding behavior for DHT, ICE/STUN/TURN, and ICE-targeted UPnP on both Host and Controller.
- Confirmed that Windows default proxy handling is limited to the centralized WinHTTP rendezvous path; ordinary HTTP/HTTPS proxy settings do not carry the current public-DHT or ICE UDP traffic. A TUN/VPN adapter remains a routed adapter case.
- Compared representative System default and explicit-physical cross-LAN evidence. Both modes have completed real media runs and both have failed under other peer/NAT conditions, so historical log-file counts are not a valid success-rate estimate.
- Added planned work and acceptance criteria for independently selectable System default, specific adapter, and accurately scoped system-proxy modes. TURN remains the dependable ICE relay fallback for hard NATs.

## 2026-09-12 — GitHub unit baseline repair

- Kept backend-independent DHT listen-port selection in the base service library so the default unit build links its matching tests without installing the optional libtorrent and miniupnpc feature set.
- Added a vcpkg binary cache keyed by the runner, vcpkg revision, manifest, configuration, and overlays. Cold or changed dependency inputs still perform one complete build; matching later jobs can reuse compiled packages.
- Aligned the public vcpkg manifest version with the `0.1.1` CMake project version.
- Preserved the non-E2E Windows unit baseline and kept physical public-DHT, desktop, GPU, UPnP, and input acceptance outside the hosted runner.

## 2026-09-12 — v0.1.1 package verification

- Documented the boundary between GitHub-hosted checks and physical desktop acceptance, including the reason a fresh hosted runner installs its own vcpkg dependencies.
- Added a release-triggered Windows package smoke that downloads the final published ZIP, verifies its checksum and required files, and runs the packaged command entry without rebuilding the program.
- Kept real capture, GPU presentation, UPnP, cross-site ICE, focus-sensitive input, and real Provider login in the local or physical acceptance scope.
- Replaced the runtime-control writer's host-speed threshold with a deterministic pre-event-loop receipt check while retaining its measured delay as test telemetry.
- Fixed strict-mode CMake preset discovery so the release publisher can call the standard build entry point when visible presets omit the optional `hidden` field.

## 2026-09-12 — Local Agent login consistency

- Made Codex CLI discovery consistent for GUI launches that do not inherit Codex Desktop's temporary PATH: an explicitly available native CLI still wins, then the current user's Codex Desktop CLI is discovered before an older PowerShell launcher fallback.
- Kept Agent account status, cancellation, and command output inside the visible settings window, displayed the selected local CLI path when an account command starts, and compacted long Provider model lists.
- Verified the focused Provider and GUI tests, published Debug, and confirmed through the standard Controller launcher that Codex reports ready with the current CLI while the account feedback controls remain visible.

## 2026-09-11 — offsite desktop stream checkpoint

- Completed one physical cross-LAN Controller-to-Host run through public DHT and a direct NAT path. ICE connected, all four negotiated channels opened, and real H.264 capture, transport, hardware decode, and GUI presentation advanced without decode or present failures.
- This checkpoint covered the real video path. Remote input, a real Agent Provider, mixed-version peers, TURN fallback, and Release qualification remain separate acceptance work.

## 2026-09-11 — v0.1.0 public prerelease

- Published a clean public-repository baseline with Apache-2.0 licensing, bilingual product documentation, a sanitized GUI screenshot, release notes, and a Windows x64 portable-package publisher.
- Removed the repository-based communication exchange and its synchronization script. Debug Bridge retains local encrypted signaling files and explicit operator transfer; the runtime DHT local record store remains part of the rendezvous implementation.
- Changed the installer workflow to manual dispatch while the Windows service package remains unsigned and incomplete for public distribution.
- Fixed the Agent account Login action so its local command output, startup errors, non-zero exits, cancellation, timeout, and recovery guidance remain visible in the settings dialog.
- Defined functional package checks as release-blocking and kept hardware-sensitive performance targets as reported engineering observations.

## 2026-09-11 — ICE UDP port and UPnP ownership

- Added one `ice_udp_port` setting shared by CLI, runtime profiles, GUI settings, diagnostics, and PowerShell launch scripts. The product default is UDP 55000; local dual-process validation uses Controller 55001.
- Reserved the configured UDP port during startup, fixed the libdatachannel port range to that value, and made conflicts fail with an explicit error.
- Moved UPnP ownership from the DHT listener to the ICE session. Mapping failures stay visible without disabling STUN, TURN, or ordinary hole punching.
- Added focused tests for defaults, overrides, invalid values, propagation, fixed ICE ranges, port conflicts, and mapping state.

## Earlier development

The pre-public private repository contains the detailed implementation history and local acceptance evidence. The public repository starts from one sanitized root commit so internal coordination records and machine-specific evidence are not published.
